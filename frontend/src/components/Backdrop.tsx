/* Animated war-room backdrop — a drifting grid, deep radial glows and faint scanlines
   behind the whole dashboard. Pure CSS (GPU transforms/opacity); see theme.css. */
export function Backdrop() {
  return <div className="backdrop" aria-hidden="true" />;
}
